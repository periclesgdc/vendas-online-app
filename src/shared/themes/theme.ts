import { buttons } from "./buttons";
import { gray, main, meutral } from "./colors";

export const theme = {
  colors: {
    gray: gray,
    neutral: meutral,
    main: main
  },
  buttons: buttons,
}

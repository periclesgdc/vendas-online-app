import { buttons } from "./buttons";
import { alert, gray, main, meutral } from "./colors";

export const theme = {
  colors: {
    gray: gray,
    neutral: meutral,
    main: main,
    alert: alert
  },
  buttons: buttons,
}
